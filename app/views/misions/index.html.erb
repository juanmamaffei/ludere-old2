<h1>¡Hay misiones que cumplir!</h1>

<div class="transitions-enabled clearfix masonry" >

<% if current_usuario.es_docente? %>
    <div class="box col1 masonry-brick">
    <div class="panel panel-default">
    <div class="panel-heading">
        <h2 class="panel-title">Crear nueva misión</h2>
    </div>
   <div class="panel-body">
    <%= link_to '+', new_mision_path, :class =>"btn btn-success" %>
   </div>
   </div></div>

<% end %>




    <% @misions.each do |mision| %>
<div class="box col3 masonry-brick">
    <div class="panel panel-default"> <!-- Agregar distinción de col* según cantidad de visitas -->
    <div class="panel-heading">
        <h2 class="panel-title"><%= mision.nombre %></h2>
    </div>
    <div class="panel-body">
<%= image_tag mision.image.url(:large), :class => "img-rounded"%><br>
<small>Nivel: <i><%= mision.nivel %></i></small>
<p><%= mision.descripcion %></p>
<%= link_to "Dar un vistazo", mision.urltemario, :class => "btn btn-default" %> <%= link_to "¡Comenzar misión!", mision, :class => "btn btn-default"  %>


<% if current_usuario.es_docente? %>

  <%= link_to "Editar", edit_mision_path(mision), :class => "btn btn-warning"  %>
  <%= link_to "Eliminar", mision, method: :delete, data: { confirm: "¿Estás seguro que deseás eliminar esta misión?" }, class: "btn btn-danger"  %>

<% end %>




    </div>
    </div>
</div>

      <!--- FALTA VERIFICAR SI ESTÁ HABILITADA Y VISIBLE
        <td><%= mision.comentario %></td>
        <td><%= mision.habilitado %></td>
        <td><%= mision.visible %></td>

        -->
    <% end %>


</div>
<br>




